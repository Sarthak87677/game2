/** Injected by vite.config.ts: short commit hash and build time, shown in the HUD so screenshots identify the build. */
declare const __TERRA_BUILD__: { commit: string; time: string; dirty: boolean };
